【阶段一：只在当前 Agent 中安装 Skill，完成中文命名、展示简介和 Skills 归类；严禁整理文件】

我的桌面上已经放置或解压了“按内容整理与自动归档 Skill｜通用 Agent 安装包”。请只完成当前 Agent 的 Skill 安装与验证。当前阶段不授权你读取、扫描、分类、重命名、移动、复制、覆盖或删除桌面、下载及任何真实文件，也不授权创建投放箱、后台监控、登录自启动或定时任务。

一、定位并核对安装源

1. 在我的桌面查找本安装包；如果只有 ZIP，先完整解压到桌面上的普通可见文件夹。
2. 确认包内存在完整的 `organize-files-by-content` 文件夹，并至少包含：
   - `SKILL.md`
   - `VERSION`
   - `agents/openai.yaml`
   - `references/`
   - `scripts/`
   - `evals/`
3. 读取 `VERSION`，本包预期版本为 `1.5.1`。
4. 完整阅读 `SKILL.md`、它指向的 `references/organize-workflow.md` 以及包根目录的 `SKILL_INTRO.txt`，只用于理解、命名、归类和安装。整理规则不构成本阶段处理真实文件的授权。
5. 不得拿桌面、下载或工作文件做测试；所有验证只能使用 Skill 自带的合成测试或临时目录。

二、查清当前 Agent 的真实安装与发现机制

1. 先识别你当前是什么 Agent 和运行环境，再检查它实际支持的用户级 Skills 安装入口、发现机制、名称与简介字段格式、调用方式、中文显示方式和已有 Skills 分类体系。不得猜固定路径，也不得套用其他 Agent 的目录结构。
2. 如果当前 Agent 支持本地 Skill，通过其真实导入入口或可发现目录完整安装 `organize-files-by-content`。
3. 必须保留以下英文技术标识，不得翻译、改名或另建第二份冲突副本：
   - 技术名称：`organize-files-by-content`
   - 文件夹名：`organize-files-by-content`
   - 标准调用名：`$organize-files-by-content`
4. 如果当前 Agent 使用不同的调用语法，可以在界面或调用层做等价适配，但不得改变 Skill 技术身份。
5. 如果当前 Agent 不支持本地 Skill、无法访问桌面安装源、缺少必要权限或需要我执行界面操作，请明确指出具体阻塞并等待；不得把“包已放在桌面”说成“已经安装”。
6. 如果已存在同名 Skill，先比较版本、文件清单和内容：本包较新且完整时原位升级；已安装版本较新时不得降级；版本相同但内容不同或来源不清时停止并报告差异；最终只保留一个能被当前 Agent 发现的有效版本。

三、根据真实功能设置中文名称和展示简介

1. 根据 Skill 的实际功能设置中文显示名称，推荐：

   `文件｜按内容整理与自动归档`

2. 英文技术标识保持不变。如果当前 Agent 支持独立的显示名称、详情页简介、标签或目录字段，应写入这些原生字段；不得为追求中文展示而破坏发现路径。
3. SkillHub 的 `summary`、`description`、概述正文和三张说明图以包内 `SKILL_INTRO.txt` 为准，不增写作者经历、营销套话或功能清单。公开简介保持简短，完整执行规则继续使用 `references/organize-workflow.md`，不得为了缩短页面而删减规则。
4. 不得把安装包原作者的个人目录、组织名称、行业专属分类或电脑路径写入通用简介。
5. 如果当前 Agent 不支持独立中文显示名称、简介、图片或 Skills 分类，必须说明不支持的字段和实际展示结果，不得声称已生效。

四、完成当前 Agent 的 Skills 归类

1. 先查看已有 Skills 的真实分类体系，再归入功能最接近的既有类别。
2. 优先选择“文件管理”；没有时再选择“效率工具”“办公自动化”或当前体系中的同义类别。
3. 不得为一个 Skill 新造整套分类，也不得按任何个人行业背景分类。
4. 如果发现机制要求 Skills 在物理目录中平铺，不要为了分类而移到不可发现的位置；使用当前 Agent 支持的目录、标签、清单或显示字段完成归类。
5. 汇报最终中文显示名称、展示简介全文、Skills 分类、分类依据和实际安装位置。

五、安全验证安装

1. 核对安装后至少包含 `SKILL.md`、`VERSION`、`agents/`、`references/`、`scripts/` 和 `evals/`。
2. 验证 Skill 能被当前 Agent 实际发现并安全调用；优先通过 Skills 列表和一次不读取真实文件的安全调用共同验证。“复制到某个目录”不等于“已经可发现”。
3. 运行 Skill Creator 的 `quick_validate.py` 或当前 Agent 的官方等效校验；依赖缺失时原样报告，等价结构校验不得冒充官方脚本通过。
4. 使用当前可用的 Python 3 解释器运行 `scripts/self_test.py` 或等效命令。测试只能使用脚本创建的临时合成文件，预期结果为 `SELF_TEST_OK`。
5. 核对安装源与安装副本的主体文件清单和 SHA-256；平台自动生成字段造成的预期差异逐项列明，其他内容不得静默变化。

六、完成报告并停止

请用中文报告：当前 Agent 与安装机制、实际安装路径和版本、中文显示名称、展示简介全文、Skills 分类及依据、发现与安全调用验证、quick_validate 或官方等效校验、自测与依赖结果、安装源和安装副本差异，并明确确认本阶段没有读取、扫描或修改任何真实文件。

只有“当前 Agent 已实际发现、中文名称和简介已生效、Skills 归类已完成、安全自测已通过”后，才可以说安装完成。报告后立即停止，不得自动进入整理阶段。下一步必须由我另行发送 `02_安装完成后_选择范围并整理到桌面_提示词.txt`。
