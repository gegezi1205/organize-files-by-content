---
name: organize-files-by-content
description: Confirm a person's occupation, role contexts, common work types, and retrieval preferences, then learn existing folder habits and inspect every in-scope file and its context to distinguish responsibility from storage purpose, preserve coherent packages, and propose evidence-based routing and naming. Use for organizing messy or established Desktops, Downloads, work folders, shared folders, other scoped computer locations, or a dedicated inbox on Windows, macOS, or Linux; also use for multi-role or historical-work separation, safe archive extraction, text-image orientation correction, content-aware naming, formal-package protection, contextual deduplication, searchable indexing, human choice for ambiguity, and verified automation of newly dropped files.
---

# 通用文件智能整理

先理解使用者的查找习惯，再整理文件。行业、项目、目录名和操作系统都不得写死。

## 首次使用

1. 确认使用者明确授权的一个或多个整理范围及排除项，可以是桌面、下载、工作资料、共享目录或电脑内其他文件夹。多个根目录分别建立配置和索引，不把“电脑内文件”理解为未经授权扫描整台电脑。
2. 本阶段完整阅读 [personal-classification-model.md](references/personal-classification-model.md) 以完成画像和分类判断。只有准备生成或校验配置时再完整阅读 [configuration.md](references/configuration.md)；只有使用者另行要求投放箱、监控或自启动时再完整阅读 [platform-automation.md](references/platform-automation.md)。不要在首次加载时无条件展开全部参考文件。
3. 在读取正文前完成“职业与身份适配”：只要求使用者自行填写职业、职位、行业或工作场景及必要的多重身份；当前、历史、兼职或临时状态、身份适用范围等其余信息提供编号选项让本人选择。再让本人选择本次范围与职业画像是否相关；选择“不相关”时记录 `profile_applies: false`，跳过工作类型候选，但仍保留范围、权限、预览和执行确认。
4. 根据本人填写的身份提出合计5—8项常见职责或工作类型候选，明确它们只是待确认假设。让本人用编号标记“主责、参与、仅接收或参考、不适用”；同时提供“已覆盖、还有遗漏请给下一组、暂不确定待盘点后再问”。输出候选后停止，未经本人回复不得进入下一步，未经确认的候选不得进入分类规则。
5. 只读盘点现有文件夹层级、文件分布、命名顺序、常用简称、版本写法和近期使用方式。
6. 抽样阅读各主要类别中的代表性文件正文；正式执行时为范围内每个文件建立处理记录。结合原父目录、相邻文件、作者或创建者、发起方、接收方、业务对象、完整稿或局部填报、正文引用和实际留存用途，不要只凭标题关键词、扩展名或修改时间判断。
7. 形成“个人分类画像”，说明：
   - 本人确认的职业或身份、状态和时段，以及各身份适用的根目录；
   - 已确认、已否定和后续候选中补齐的工作类型，典型业务对象、交付物、接收方与工作周期；
   - 使用者通常按项目、客户、事项、内容类型、人员、阶段还是时间查找；
   - 哪些原有目录和命名必须保留；
   - 责任角色与实际存放用途如何分别判断；
   - 独立项目、稳定工作域、总结计划、会议材料、重要汇报或报送、参考资料、个人材料等用途如何区分；
   - 项目、事项、简称、编号、人员、组织、关键词、上下文证据和排除词；
   - 跨项目参考资料的内容类别及边界；
   - 建议的目录树、命名格式、重复与版本政策。
8. 判断原有分类是否存在稳定意图：
   - 同类文件反复聚合在同一位置、目录名称含义一致、命名规律或邻接关系重复出现时，优先沿用；
   - 同类文件四处散落、目录互相冲突、一次性文件夹过多时，不把混乱误当成习惯；
   - 桌面或其他位置几乎全是零散文件、无法恢复可靠意图时，明确说明证据不足；先识别受保护包和低风险聚类，再按实际用途提出少量候选结构，把不确定项保持原位并分批预览，不得强套项目结构。
9. 准备生成 `00_整理说明/整理配置.json` 时完整阅读 `configuration.md`，再展示身份确认记录、分类树、证据、一级目录数量和至少5个真实改名示例。一级入口由原有查找习惯、稳定证据和可扫描性共同决定；不得无证据增加“综合工作、WORK、智能归档、杂项”等空泛包裹层，也不得把所有项目、工作域和内容类别机械拆到根目录。
10. 未经使用者确认，不批量移动原有文件，不安装自动监控。只有使用者另行要求自动化时才读取 `platform-automation.md` 并执行相应门槛。

详细决策表和混乱目录兜底步骤见 [personal-classification-model.md](references/personal-classification-model.md)。

## 职业与身份适配

职业、职责、业务对象、成果、接收方、周期、协作关系、原目录和正文内容都是判断元素。把它们的组合关系还原成“身份 → 工作对象 → 常见工作类型 → 典型成果与接收方 → 周期或阶段 → 首要查找入口”的全景工作模型；职业信息只用于生成问询候选、理解专业语境和检查职责覆盖，不是目录模板。

- 只让本人自行填写职业、职位、行业或工作场景及必要的多重身份；职业可用宽泛表述，不要求填写单位名称或无关个人信息。除 Agent 无法检出的必要完整路径外，其他判断元素全部由 Agent 给出短选项，不得要求使用者自由填写或撰写说明。
- 对每个身份记录当前、历史、兼职、副业、临时或非职业长期角色及大致有效时段；未知时段可留空，不用 mtime 推断身份时期。
- 身份确认是两级硬门槛。收到职业或身份后，第一轮只提出合计5—8项编号候选，使用本人能理解的任务或成果语言，不写成目录名；表格至少包含“关联身份、候选工作类型、为什么提出、选择代码”。然后要求本人用 `主/参/阅/否` 或界面选项逐项确认并停止，不得在同一轮继续提出目录树、命名方案或分类规则。本人明确回复后才可进入只读盘点；选择“暂不确定，待盘点后再问”只授权只读盘点，暂缓项解决或明确排除前不得生成正式路由、目录方案或执行配置。
- 本人确认时记录候选来源、主责/参与/仅接收或参考/不适用、长期/阶段性/一次性属性；未确认和“不适用”项不得形成关键词、路由或隐性权重。候选未覆盖时，让本人只选择“还有遗漏，请给下一组”，由 Agent 继续提供3—5项新候选，不要求本人自行命名；也可选择“暂不确定，待盘点后再问”。
- 主要业务对象、典型交付物及接收方、工作周期、首要查找入口和正式包或敏感资料边界，也由 Agent 根据已确认身份和工作类型生成短编号选项。每轮控制在一个短屏内，并提供“全部确认、换一组、盘点后再确认”，只收集整理所需的最少信息。
- 只读盘点后，将实际出现的内容与画像对照：让本人确认未预见的工作是当前职责、历史岗位、临时任务、个人事务、他人材料还是仅供参考。文件证据与职业常见推断冲突时，以正文用途、稳定上下文和本人确认结果为准。
- 每个授权根目录标明个人工作、个人私密、团队共享或混合，以及本人允许的读取、改名、移动和去重权限。职业画像只作用于本人确认的范围；共享目录的团队规则和他人权限优先。
- 一个配置只能覆盖一组一致权限。混合范围默认只读，实际执行前按个人、共享和权限边界拆成多个明确根目录；团队共享范围必须另行确认写入授权。内置整理脚本只处理配置根目录内的文件，不能承担多个电脑位置统一迁移到桌面；跨根迁移由当前 Agent 按已确认的全量预览逐项执行和验收。
- 多重身份或一份材料服务多个身份时，仍按实际用途和本人首要查找入口决定存放。两种解释均合理时展示候选并保持原位，不创建“职业一、职业二”式默认一级目录。
- 允许从局部元素逐步还原整体工作体系，但每一步都要标注证据、置信度和缺口。至少两类相互独立的判断元素互相印证后，才形成稳定规则；单一职业、单一关键词或单一文件只能生成候选，出现反证时降低置信度并展示冲突。

详细问询模板、证据优先级和边界见 [personal-classification-model.md](references/personal-classification-model.md)。

## 责任、用途与存放维度

先判断材料中的责任角色，再单独判断使用者以后会按什么维度查找。主责、牵头、协同、配合、作者或供稿人只说明责任，不直接决定一级目录；主责不等于独立项目。本人确认的职业和工作类型属于辅助上下文，同样不能单独决定存放维度。

实际存放用途可能是独立项目、稳定工作域、总结计划、会议材料、重要汇报或报送、参考资料、个人材料等。这里列的是判断维度，不是预置目录；名称和层级必须从现有聚类、上下文和本人选择中归纳。

### 独立项目门槛

独立项目通常须同时具备：

- 可独立识别的事项身份，例如稳定专名或编号；
- 成组材料、阶段链条或较完整生命周期中的至少一种；
- 持续查找价值，且项目确实是使用者的主要检索入口。

仅有一项任务、负责人、交付件、一次性写作材料，或单份总结、讲话页、会议供稿，不足以自动成为一级项目。一级目录因项目判断而暴增、难以扫描，视为验证失败。

### 稳定类别与目录层级

稳定工作域或有证据的内容类别可以承载主责工作。多文件、可持续查找的事项可在其下形成带可靠年月或时段的子项目包；普通单文件通常直接放入最合适的稳定类别，不为每个文件新建文件夹。

原有重复聚类和邻接关系是强证据。不得把原本聚合的同类内容机械拆成大量一级目录；改变核心分类逻辑前必须展示旧逻辑、新逻辑、代表性路径和一级目录数量，等待确认。

### 用途和上下文优先

- 用途和上下文证据高于标题中的组织名及“总结、规划、报告、会议”等普通词。同名组织可能是作者、发起方、接收方、业务对象或仅被引用者，须结合全文和相邻材料判断身份。
- 每份材料必须分别判断：来源/作者角色、实际业务对象、材料体裁、发起方与接收方、
  责任关系、战略或业务关联层级、真实留存用途、原路径/包/邻接上下文、时间版本，
  以及反证冲突。证据来源数量和语义维度数量是两件事；标题与正文重复同一句话，
  不能冒充两个判断维度。
- 关联层级至少区分“直接业务、间接支撑或约束、组织级战略与治理、确有用途证据的
  外部参考”。没有出现某个专业词，不等于与该工作无关；缺少主业词不是外部参考证据，
  看起来与主业不直接相关也不等于保存用途就是表达参考。
- 已位于边界清楚、长期稳定目录中的文件，原路径和邻接关系是强证据。单独出现的文档或演示稿也可能只是会议、报送、流程或正式包的一部分，应通过正文引用、作者、时间、内容相似和相邻材料恢复上下文。
- 讲话、正式发布材料、会议精神、下发公文、规划制度和上游材料可因实际留存用途归参考资料；本人撰写或供稿也可能是主责，但应按总结计划、重要汇报等真实用途存放，不自动成为一级项目。
- 项目包、会议包、正式报送包、证据包、导出包和程序运行目录优先保持正式名称、内部文件和相对结构完整；不得逐个抽走或跨包去重。
- 自动脚本只在文件位于本人确认的保护路径，或某级目录精确命中“正式提交包、会议包、报送包、证据包、运行目录”等包/运行目录标记（可带可靠日期后缀）时原位登记；不得用子串把“某包管理制度研究”等普通目录误认成包。配置中只写“保护包结构”而运行时仍抽取文件，视为失败。

### 冲突与纠错

- 独立项目、稳定工作域和真实用途要相互比较。稳定原路径可以覆盖“看起来像项目”的弱信号；强项目身份也不能跳过用途与查找入口验证。
- 同时存在多种解释、责任与用途冲突、同名组织身份不清、项目门槛不足或材料性质不清时，不得猜测。交互运行时展示 2—3 种解释、各自证据和影响，等待选择后再移动；未选择前文件保持原位。
- 职业候选与正文用途、历史时段、共享规则或原有路径冲突时，不得为迎合职业画像而硬分。未确认或被本人否定的职业候选不参与分类评分。
- 无人值守监控遇到上述情况时，发送“需要选择归类”的桌面提醒并把候选项写入待办记录，文件继续留在投放箱，不得先移入某个分类。下次智能体运行时再次向使用者提问。
- 不得为了提高自动归档比例而降低置信度门槛。
- 不得用兜底推理压缩待确认，例如“不是主业材料，所以一定是参考资料”。应先比较
  直接业务、间接关联、组织战略治理、项目/会议/报送用途和外部参考；仍缺少用途或
  上下文证据时才进入待确认，并明确缺的是哪个维度。
- 一次纠错如果揭示的是通用判断错误，必须审计全范围内具有相同证据模式的文件，不能只修被点名的一项。

使用者没有选择前，不得创建临时目标路径、不得复制到候选目录，也不得用“其他、综合、杂项”代替选择。

## 命名

默认使用：

`主题或项目_具体事项_材料性质_版本_日期.ext`

- 主题或项目在前，日期默认放末尾；个人配置可以调整，但不得未经确认改变原有习惯。
- 日期最多精确到月份，统一写作 `YYYY-MM`；年份资料可以只写 `YYYY`，不得在自动命名中加入具体日。
- 日期依据顺序为：正文落款或封面成文日期、正式元数据、文件名中的可靠日期。正文与文件名冲突时以正文为准；没有可靠日期时不强加日期，不能把文件修改时间冒充成文日期。
- 保留明确的 `V2`、`终版`、`定稿`、`正式版`、`盖章版`。
- 清理下载序号、聊天图片名、裸 `MMDD`、模糊占位名以及“新建文档、文件、副本、copy”等无意义词；证据可靠时补充主题、材料类型、版本和年月。
- 父目录已经说明组织背景时，文件夹默认使用“核心事项 + 可靠年月或时段”，避免逐层重复组织名。组织名只在正式专名、区分来源或接收方、说明范围或保护正式包时保留。
- 沿用使用者熟悉的简称、语言和目录深度，让文件脱离文件夹后仍能理解，但不得机械添加组织前缀。
- 范围内每个普通业务文件都必须经过正文命名审计。只要原名含日期前置、具体到日、无意义序号、下载尾缀、模糊占位词、重复主题、缺少可识别主题或与正文主题不符，就要按正文重命名。
- 普通文件只有在原名已经准确表达主题、事项、材料性质、必要版本和可靠月份，并符合个人命名顺序时，才可保持原名。处理记录必须写“名称已合规”及判断依据，不能用笼统的“保留”代替命名审计。
- 不得为了证明执行过而把一个已经准确合规的名称机械改成同义词；也不得因为文件已在正确目录就跳过命名审计。
- 中文括号、书名号、引号、标点和引号内的固定短语属于名称语义的一部分。除跨平台非法字符外，不得仅为格式统一而改成另一套标点或把固定短语中的空格机械替换为下划线。
- `2024-2025`、`2026上半年`、`2026Q2` 等时间跨度或周期必须作为一个整体保留，不能截成单一年份。日期精度压缩到月份后若多份不同内容会重名，优先使用正文已有的期次或版本区分；没有可靠期次或版本时转为待人工选择，不得覆盖或自动追加无含义序号。
- JPEG、PNG、WebP 文字图片先比较 0°、90°、180°、270° 的 OCR 结果。只有文字量和识别质量具有明确优势时才纠正方向；正常图片、照片和低文字图片保持不动。发生旋转后必须重新提取正文、重建内容名称，并按新文件重新计算大小和 SHA-256；解码、OCR 或安全写回不可用时保留原图并明确记录。

### 不改名边界

以下文件仍要读取、分类和登记，但默认保持正式名称及包内相对路径。这些是“正式名称保护”，不是遗漏改名：

- 制度、办法、规定、细则、会议机制等具有正式全称的文件；
- VIS/VI/视觉识别系统手册、标志集合、标准模板等标准化品牌资产；
- OA印发通知、正式公文及其导出的正文、红头、表单、附件和压缩包；
- 申报报奖、巡视巡察、审计、验收等已成套提交或取证的材料包；
- 软件、脚本、模型、渲染、解压目录等会因改名而破坏依赖关系的运行文件。

只有使用者明确要求，或能证明名称只是临时占位且不会破坏正式引用时，才可修改上述名称。不得为了格式整齐而改写正式文件名。

## 自动投放箱

配置确认并通过预演后，只有使用者另行选择启用监控，才在根目录创建：

`00_待归档`

`schema_version: 3` 的新配置默认使用该名称。已有 `schema_version: 2` 配置继续沿用
原来的 `待智能整理`，不得为了升级强制移动既有投放箱。

使用者只需把新文件放入该文件夹。跨平台监控程序调用：

```bash
python scripts/organizer.py \
  --config "<整理配置完整路径>" \
  --apply \
  --file "<新增文件完整路径>"
```

高置信度文件只有在本次文件实际命中至少两类相互独立证据时才自动改名归档；配置里声明“至少两类”不能代替正文、正式元数据、已确认稳定原路径或文件名等真实命中。随机父目录和随机邻居可供 Agent 在预览中提出候选，但不能直接充当自动路由证据。无法可靠读取或判断的文件保持在投放箱，并在 `00_整理说明/99_待人工选择.csv` 中列明 2—3 个候选分类、判断依据和原路径。交互运行时必须弹出选择，选择完成后才归档。使用 [watch_inbox.py](scripts/watch_inbox.py) 监控投放箱，并按 [platform-automation.md](references/platform-automation.md) 设置登录后自动运行。

一次性整理确认不能替代监控授权。前台监控须单独记录启用确认；至少一次真实投递完成接收、执行、落位和索引登记后，再由使用者单独确认登录自启动。脚本会拒绝未确认画像、只读或混合权限、未确认全量预览、待选择项未清空、根目录外路径、未单独授权监控或未通过真实投递测试的配置。

### 压缩包与媒体授权

- 始终识别 ZIP、TAR/TGZ/TBZ/TXZ、7Z 和 RAR。ZIP/TAR 使用内置安全解析；7Z/RAR
  使用系统中实际检测到的 `bsdtar` 或 7-Zip。缺少安全解压器时明确说明，不把压缩包
  误报为普通不支持文件。
- 拒绝绝对路径、`..` 越界、软硬链接、特殊文件、程序脚本、可执行成员、加密包、
  损坏包和超限包。固定上限为 1000 个成员、单文件 1GB、解压总量 2GB、嵌套两层。
- 逐个读取包内主件。真实业务路线一致时整包自动归档，附件随包保留；存在相互冲突
  的业务路线时整包保持完整并待选择，不拆散。
- 归档结构固定为 `材料包/00_原始压缩包` 与 `材料包/01_解压内容`，保留内部相对
  结构。原包、包内主件和附件分别进入索引。原包名称有意义时保留；哈希、下载序号
  或占位名按正文生成可识别的材料包名称，但原压缩包文件名在包内不变。
- 新配置必须使用 `schema_version: 3`，并在 `media_processing.authorized_actions`
  同时记录 `extract_archive`、`rotate_text_image` 及一次性 `confirmed_at`。旧版
  schema 2 仍处理普通文件；补充授权前不自动解压压缩包，也不修改图片。
- 普通 DOCX、PDF、PPTX、图片和文本继续逐个自动处理，不因“不是 ZIP”进入待确认。
  Finder 元数据、Office 锁文件和下载中临时项保持原位，登记为运行依赖，不计入未解决
  业务材料。只消除可由完整正文和上下文解决的待确认，不降低真实语义冲突门槛。

## 重复与版本

- SHA-256相同只证明内容相同，不证明留存用途相同。先比较原路径、包上下文、提交或证据角色、会议或报送关系和参考用途。
- 普通工作区中用途相同且没有包语境的无意义重复只保留一份，不创建“重复件”目录；mtime最多用于同内容普通副本的留存选择，不能判断业务日期或版本新旧。
- 正式封装、提交、证据、会议、报送和参考情境中的相同文件可能承担结构完整性或追溯作用。自动保留须有精确包边界或本人确认的情境路径，并在索引标记“情境副本”；两个不同嵌套路径的用途关系未经确认时保持原位待选择，不得跨包删除，也不得仅凭“会议、参考”等目录子串永久保留。
- 同一主题但内容不同，仅在日期、版本号或定稿状态能明确证明新旧时替换。
- 新旧关系不明确时进入待人工选择，禁止自动覆盖或追加 `_2` 并列归档。
- 被替换的旧版按个人配置进入历史版本区或回收站，并保留索引记录。

## 索引与验收

索引必须覆盖范围内每个文件，包括保留原名、包内副本、运行依赖、系统文件、不支持格式和待人工选择项。至少记录：原路径、新路径、项目、非项目内容类别、材料性质、内容摘要、分类依据、处理决定、置信度、日期、版本、大小和SHA-256。可读取文件不得缺少哈希；无法读取的文件须以“失败”登记路径、大小和阻塞原因，并阻断“整理完成”声明，不能静默漏记。隐藏文件和暂不处理项即使由监控发现也须登记为运行依赖；同一实体重复运行只保留一条当前活动记录，已归档文件若被原地改写，旧哈希记录必须失效，过程日志另计。

处理决定必须细分为：`已按内容改名`、`名称已合规`、`正式名称保护`、`归位`、`包内副本`、`运行依赖`、`待人工选择`、`明确新版替换`。普通文件不得只记录为无法判断是否审过名称的“保留”。

验收必须确认：

- 职业、身份状态和工作类型候选已经由本人确认；未确认或“不适用”项没有进入路由，历史身份、个人资料和共享范围没有被当前职业覆盖；
- 使用者已确认全量处理预览，暂缓项和待选择项已经解决或明确排除；仅回复“暂不确定”不构成执行授权；
- 每个执行配置只有一组一致权限；混合范围已经拆分，团队共享范围另有明确写入授权；
- 使用者能按原有思路快速找到抽样文件；一级入口有稳定证据且数量可扫描，不存在人为增加的空泛业务总包；
- 一级目录数量可扫描；旧路由、失效路径和无证据空泛层已清零，原有合理聚类没有被机械拆散；
- 路径存在，按状态核算后的活动索引记录数与实体文件一致；已合并重复、历史版本和失败记录另行核对，所有可读取记录的大小和SHA-256一致；
- 项目资料和跨项目内容资料边界清楚；
- 每个普通文件都完成命名审计；应改名项已按正文改名，未改名项有“名称已合规”或“正式名称保护”的明确依据；
- 文件名主题在前、日期位置符合个人配置；
- 没有普通重复件目录，没有静默覆盖；普通重复与包内或其他情境副本已区分；
- 日期不超过月份，且抽查正文日期与文件名一致；
- 制度、VIS、OA通知、正式材料包和运行依赖的名称与结构没有被误改；
- 待人工选择文件保持原位，列出 2—3 个候选项及原因；不得先移动再询问；
- 分类体系变化后已重建配置，旧路径状态已归档；覆盖责任与用途冲突、项目门槛、正式包、情境副本和待确认的代表性预演通过；
- 安装、配置、启用和真实运行分别记录状态；自动监控只有在至少一次真实投放、自动执行、目标落位和索引登记全部通过后，才可声明运行成功。
